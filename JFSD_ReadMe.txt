GIT tutorial
App architecture
Angular Tutorial.
Resume - Raytheon.
Tanish College Stuff Prep.

Non 30 2025
***********
Page 101- Before the text - Creating the components - Date Nov 29 2025
Page 115- Date Nov 30 2025
Angular Directives. - ngFOr, ngSwitch and NgIF
Component directive, Structural Directive and Attribute Directive.
Pipe directives ng g p pipeName. Use the transform to add intelligence to the pipe.
Angular pipes - Built in pipes and Custom Pipes.



Non 29 2025
***********
App module is the main feature of the entire angular application.
Create cmd terminal at the first app folder
tf = Template Reference
two-way-demo
property-demo
event-demo






Until Nov 28 2025
*****************
C:/Jaggi/Education/eBooks/simplilearn-weekend-oct25
git pull origin master
git status
git log --oneline
git add .
git commit -m 'message'
git push -u origin master
git push -u origin <branchname>

To delete the local remote-tracking branch that no longer exists remotely, use git fetch --prune or git fetch -p.
To delete the local branch itself, you can use git branch -d <branch-name> or git branch -D <branch-name> to forcefully delete it. 

Working with Branches
git checkout -b <branch_name>
git add .
git commit -m  "added section"
git checkout master
git merge <branch_name>




git branch <branch_name> = creates a new branch
git checkout
git add .
git commit -m ""
git push -u origin <branch_name>
git merge master
git status
To clean up your local repository 
To delete the local remote-tracking branch that no longer exists remotely, use git fetch --prune or git fetch -p.
To delete the local branch itself, you can use git branch -d <branch-name> or git branch -D <branch-name> to forcefully delete it. 
